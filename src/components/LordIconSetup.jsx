import { useEffect, useRef, useState } from "react";
import { Player } from "@lordicon/react";

const LordIconSetup = () => {
     const [size, setSize] = useState(0);
  const playerRef = useRef(null);
  return (
    <div>LordIconSetup</div>
  )
}

export default LordIconSetup